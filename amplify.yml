version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step needed - static site"
    build:
      commands:
        - echo "Build complete"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
